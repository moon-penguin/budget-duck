model Position {
  title    String
  category String[]
  value    Float
  cycle    Cycle
  type     Type
  date     DateTime
  id       Int      @id @default(autoincrement())
}

enum Cycle {
  ONCE
  DAILY
  WEEKLY
  MONTHLY
  YEARLY
}

enum Type {
  INCOME
  EXPENSE
}
